# Cargo manifest — also doubles as the TOML sample for `rust/`.
# The real TOML token sample is ../08-config.toml.

[package]
name = "preview"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]

[profile.release]
opt-level = 3
lto = true
